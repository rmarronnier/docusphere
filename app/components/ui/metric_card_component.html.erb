<div class="<%= card_classes %>">
  <div class="flex items-center">
    <% if icon.present? %>
      <%= render Ui::IconComponent.new(name: icon, css_class: "h-8 w-8 #{icon_color}") %>
    <% end %>
    <div class="<%= icon.present? ? 'ml-4' : '' %> flex-1">
      <h3 class="text-lg font-semibold <%= value_color %>"><%= formatted_value %></h3>
      <p class="text-sm text-gray-600"><%= title %></p>
      <% if subtitle.present? %>
        <p class="text-xs text-gray-500 mt-1"><%= subtitle %></p>
      <% end %>
    </div>
    <% if trend.present? %>
      <div class="ml-2 flex items-center">
        <%= render Ui::IconComponent.new(name: trend_icon, css_class: "h-5 w-5 #{trend_color}") %>
        <% if trend_value.present? %>
          <span class="text-sm <%= trend_color %> ml-1"><%= trend_value %></span>
        <% end %>
      </div>
    <% end %>
  </div>
</div>