<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Immo::Promo - Docusphere" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="<%= asset_path('icon.png') %>" type="image/png">
    <link rel="icon" href="<%= asset_path('icon.svg') %>" type="image/svg+xml">
    <link rel="apple-touch-icon" href="<%= asset_path('icon.png') %>">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <div class="min-h-screen" data-controller="immo-promo-navbar">
      <!-- Navigation -->
      <%= render Immo::Promo::NavbarComponent.new(
        current_user: current_user,
        current_project: @project
      ) %>
      
      <!-- Main content -->
      <main>
        <!-- Flash messages -->
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
          <%= render_flash_messages %>
        </div>
        
        <!-- Page content -->
        <div class="py-6">
          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>