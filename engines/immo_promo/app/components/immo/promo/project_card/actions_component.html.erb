<div class="flex justify-between items-center pt-4 border-t border-gray-200">
  <%= link_to "Voir détails", project_path, 
              class: "text-indigo-600 hover:text-indigo-900 text-sm font-medium" %>
  <div class="flex space-x-2">
    <% if helpers.policy(project).edit? %>
      <%= link_to "Éditer", edit_project_path, 
                  class: "text-gray-400 hover:text-gray-600" %>
    <% end %>
    <% if helpers.policy(project).destroy? %>
      <%= link_to "Supprimer", project_path, method: :delete,
                  confirm: "Êtes-vous sûr de vouloir supprimer ce projet ?",
                  class: "text-red-400 hover:text-red-600" %>
    <% end %>
  </div>
</div>