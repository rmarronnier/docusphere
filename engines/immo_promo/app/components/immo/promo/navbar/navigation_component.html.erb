<div class="hidden sm:ml-6 sm:flex sm:space-x-8">
  <%= link_to "Tableau de bord", projects_path,
              class: "#{on_dashboard? ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
  
  <%= link_to "Projets", projects_path,
              class: "#{on_projects_index? ? 'border-blue-500 text-gray-900' : 'border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700'} inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
  
  <% if current_project %>
    <span class="inline-flex items-center px-1 pt-1 text-sm text-gray-400">/</span>
    <%= link_to current_project.name, project_path,
                class: "border-blue-500 text-gray-900 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-medium" %>
  <% end %>
</div>